# 🔧 Ручная настройка ngrok

## Проблема
ngrok не запускается автоматически. Нужно запустить его вручную.

## Решение

### Шаг 1: Запустите ngrok вручную
1. Откройте новое окно командной строки
2. Перейдите в папку проекта: `cd d:\web3\gemblyshka`
3. Запустите ngrok: `ngrok http 5173`
4. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

### Шаг 2: Обновите .env файл
В файле `d:\web3\gemblyshka\server\.env` замените:
```
WEBAPP_URL=https://your-ngrok-url.ngrok.io
```
на ваш реальный ngrok URL:
```
WEBAPP_URL=https://abc123.ngrok.io
```

### Шаг 3: Запустите сервер
В другом окне командной строки:
```bash
cd d:\web3\gemblyshka\server
node index.js
```

### Шаг 4: Запустите фронтенд
В третьем окне командной строки:
```bash
cd d:\web3\gemblyshka
npm run dev
```

### Шаг 5: Настройте BotFather
1. Откройте [@BotFather](https://t.me/BotFather)
2. `/mybots` → выберите бота → "Bot Settings" → "Menu Button"
3. Введите ваш ngrok URL

## Альтернативное решение

Если ngrok не работает, можете использовать:
- **Cloudflare Tunnel** (бесплатно)
- **Vercel** (для фронтенда)
- **Railway** (для полного стека)

## Проверка работы
1. Откройте бота в Telegram
2. Нажмите кнопку "🚀 Запустить бота"
3. Приложение должно открыться внутри Telegram
