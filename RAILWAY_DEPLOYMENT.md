# 🚀 Деплой на Railway

Railway - это современная платформа для деплоя приложений, которая автоматически настраивает все необходимое.

## Преимущества Railway

- ✅ **Простота** - один клик для деплоя
- ✅ **Автоматический HTTPS** - SSL сертификат из коробки
- ✅ **Автоматическое масштабирование**
- ✅ **Встроенная база данных** (если нужно)
- ✅ **Переменные окружения** через веб-интерфейс
- ✅ **Мониторинг** и логи
- ✅ **Бесплатный тариф** для начала

## Быстрый деплой

### 1. Подготовка проекта

Проект уже готов! Файлы созданы:
- ✅ `railway.json` - конфигурация Railway
- ✅ `Procfile` - команда запуска
- ✅ `package.json` - обновлен с командой `start`
- ✅ `server/index.js` - настроен для статики

### 2. Создание аккаунта

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите "Login" → "GitHub"
3. Авторизуйтесь через GitHub

### 3. Деплой проекта

#### Вариант A: Через GitHub (рекомендуется)

1. **Загрузите проект на GitHub**:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/your-username/ai-photo-analyzer.git
   git push -u origin main
   ```

2. **Подключите к Railway**:
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий
   - Railway автоматически определит настройки

#### Вариант B: Прямая загрузка

1. **Создайте новый проект**:
   - Нажмите "New Project"
   - Выберите "Deploy from folder"
   - Загрузите папку проекта

### 4. Настройка переменных окружения

В панели Railway:

1. Перейдите в ваш проект
2. Нажмите на сервис
3. Перейдите в "Variables"
4. Добавьте переменные:

```
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
WEBAPP_URL=https://your-app-name.railway.app
PORT=3001
```

### 5. Настройка домена (опционально)

1. В панели Railway перейдите в "Settings"
2. В разделе "Domains" добавьте ваш домен
3. Railway автоматически настроит SSL

### 6. Настройка BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. `/mybots` → выберите бота → "Bot Settings" → "Menu Button"
3. Введите URL вашего Railway приложения:
   ```
   https://your-app-name.railway.app
   ```

## Мониторинг

### Логи
- В панели Railway перейдите в "Deployments"
- Нажмите на последний деплой
- Просматривайте логи в реальном времени

### Метрики
- CPU и память
- Сетевой трафик
- Время отклика

### Перезапуск
- В панели Railway нажмите "Redeploy"
- Или измените переменную окружения

## Обновление приложения

### Через GitHub
```bash
git add .
git commit -m "Update app"
git push origin main
```
Railway автоматически пересоберет и перезапустит приложение.

### Через Railway CLI
```bash
# Установите Railway CLI
npm install -g @railway/cli

# Логин
railway login

# Деплой
railway up
```

## Troubleshooting

### Проблема: Приложение не запускается
- Проверьте логи в панели Railway
- Убедитесь, что все переменные окружения установлены
- Проверьте, что порт указан правильно

### Проблема: Статические файлы не загружаются
- Убедитесь, что `npm run build` выполняется
- Проверьте, что папка `dist` создается
- Проверьте настройки в `server/index.js`

### Проблема: Telegram бот не работает
- Проверьте `TELEGRAM_BOT_TOKEN`
- Убедитесь, что `WEBAPP_URL` указывает на Railway домен
- Проверьте логи сервера

## Стоимость

- **Бесплатный тариф**: $5 кредитов в месяц
- **Pro тариф**: $20/месяц за неограниченное использование
- **Плата за ресурсы**: CPU, память, сеть

Для Telegram бота бесплатного тарифа должно хватить!

## Альтернативы Railway

Если Railway не подходит:
- **Vercel** - для фронтенда + API
- **Render** - полный стек
- **Heroku** - классический выбор
- **DigitalOcean App Platform** - VPS альтернатива

## Следующие шаги

1. ✅ Деплой на Railway
2. ✅ Настройка переменных окружения
3. ✅ Настройка BotFather
4. ✅ Тестирование приложения
5. 🎉 Готово!

Railway - это самый простой способ запустить ваше приложение в продакшене! 🚀
